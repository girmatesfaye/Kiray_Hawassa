diff --git a/node_modules/expo-server/build/cjs/vendor/http.js b/node_modules/expo-server/build/cjs/vendor/http.js
index 4ffaac4..020150b 100644
--- a/node_modules/expo-server/build/cjs/vendor/http.js
+++ b/node_modules/expo-server/build/cjs/vendor/http.js
@@ -46,7 +46,7 @@ function createRequestHandler(params, setup) {
         try {
             const request = convertRequest(req, res);
             const response = await requestHandler(request);
-            await respond(res, response);
+            await respond(res, response, { signal: request.signal });
         }
         catch (error) {
             // http doesn't support async functions, so we have to pass along the
@@ -68,6 +68,7 @@ function convertRequest(req, res) {
     // Abort action/loaders once we can no longer write a response
     const controller = new AbortController();
     res.on('close', () => controller.abort());
+    res.once('error', () => controller.abort());
     const init = {
         method: req.method,
         headers: convertRawHeaders(req.rawHeaders),
@@ -97,7 +98,10 @@ function convertHeaders(requestHeaders) {
     }
     return headers;
 }
-async function respond(res, expoRes) {
+async function respond(res, expoRes, options) {
+    if (res.writableEnded || res.destroyed) {
+        return;
+    }
     res.statusMessage = expoRes.statusText;
     res.statusCode = expoRes.status;
     if (typeof res.setHeaders === 'function') {
@@ -109,7 +113,7 @@ async function respond(res, expoRes) {
         }
     }
     if (expoRes.body) {
-        await (0, promises_1.pipeline)(node_stream_1.Readable.fromWeb(expoRes.body), res);
+        await (0, promises_1.pipeline)(node_stream_1.Readable.fromWeb(expoRes.body), res, { signal: options?.signal });
     }
     else {
         res.end();
diff --git a/node_modules/expo-server/build/mjs/vendor/http.js b/node_modules/expo-server/build/mjs/vendor/http.js
index be9397c..61d1dce 100644
--- a/node_modules/expo-server/build/mjs/vendor/http.js
+++ b/node_modules/expo-server/build/mjs/vendor/http.js
@@ -38,7 +38,7 @@ export function createRequestHandler(params, setup) {
         try {
             const request = convertRequest(req, res);
             const response = await requestHandler(request);
-            await respond(res, response);
+            await respond(res, response, { signal: request.signal });
         }
         catch (error) {
             // http doesn't support async functions, so we have to pass along the
@@ -60,6 +60,7 @@ export function convertRequest(req, res) {
     // Abort action/loaders once we can no longer write a response
     const controller = new AbortController();
     res.on('close', () => controller.abort());
+    res.once('error', () => controller.abort());
     const init = {
         method: req.method,
         headers: convertRawHeaders(req.rawHeaders),
@@ -89,7 +90,10 @@ export function convertHeaders(requestHeaders) {
     }
     return headers;
 }
-export async function respond(res, expoRes) {
+export async function respond(res, expoRes, options) {
+    if (res.writableEnded || res.destroyed) {
+        return;
+    }
     res.statusMessage = expoRes.statusText;
     res.statusCode = expoRes.status;
     if (typeof res.setHeaders === 'function') {
@@ -101,7 +105,7 @@ export async function respond(res, expoRes) {
         }
     }
     if (expoRes.body) {
-        await pipeline(Readable.fromWeb(expoRes.body), res);
+        await pipeline(Readable.fromWeb(expoRes.body), res, { signal: options?.signal });
     }
     else {
         res.end();
